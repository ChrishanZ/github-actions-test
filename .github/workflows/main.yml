name: Mon premier workflow
on: [push]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World!"  
  say-hello2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World!"

